# Fill in and rename to .env — this file is git-ignored via the repo's existing patterns.
# Deploy funds native RITUAL from this key.
PRIVATE_KEY=0x...

# Optional — where the 25% platform fee lands. Defaults to deployer if unset.
# PLATFORM_WALLET=0x...

# Optional — roster size (988 = current live Genesis holders as of 2026-07-08)
# MAX_MEMBER_ID=988

# Optional — platform fee in basis points. 2500 = 25%. Hard cap 3000.
# PLATFORM_FEE_BPS=2500
